<%= simple_form_for([dream, review]) do |f| %>
  <div class="form-group h-15">
    <%= f.input :comment, label: "Comment" %>
    <%= f.input :rating, label: "Rating" %>

  </div>
  <div class="actions">
    <%= f.submit "Create Review", class: "btn btn-flat" %>
  </div>
<% end %>
