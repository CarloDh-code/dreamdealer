<h1 class="text-center my-4">Ready to dream?</h1>

<div class="container">
  <div class="row">
    <% @dreams.each do |dream| %>
      <div class="col-12 col-sm-6 col-md-4 col-lg-3 mb-4">
        <%= link_to dream_path(dream), class: "text-decoration-none" do %>
          <div class="card h-100" data-aos="zoom-in">
          <%= cl_image_tag(dream.photo.key, crop: :fill, class: "card-img-top", alt: dream.title) %>
            <div class="card-body text-center">
              <h4 class="card-title"><%= dream.title %></h4>
              <p class="card-text">
                <strong><%= dream.price_per_night %>€</strong> per night
                <br>
                4.5 <i class="fa fa-star"></i>
              </p>
              <p class="text-secondary">Hosted by: <strong><%= dream.user.nickname %></strong></p>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
